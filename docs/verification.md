# 検証記録

対象：v0.1.0 初回公開準備。実施日：2026-09-12。ローカル検証環境：macOS、Node.js 24.16.0。

| 検証 | 結果 / 境界 |
| --- | --- |
| Node.js 自動テスト | 7 ケース：署名・クレーム・許可対象、D1 保存、不正アクセス、アーカイブ整合性 |
| 型チェック | 成功 |
| ビルド | 成功 |
| 文書リンク | 相対リンク・コードフェンスを検査 |
| 公開ファイル | staged ファイルと ZIP 内の対象パターンを検査。完全な秘密検出保証ではない |
| クリーン導入 | 成功。公開予定ファイルだけを別ディレクトリへ展開し、npm ci、実起動、プロキシ、架空データの CLI import、D1 の再起動後保持を確認 |
| Google OAuth | 署名検証の自動試験と未設定ログイン画面は確認。実アカウント popup 成功は未検証 |
| 全画面 UAT | 未完了。中国語 UI・アクセシビリティに残課題あり |
| Windows / WSL2 | 導入手順を用意。実機試験は未実施 |

公開 GitHub Actions は Ubuntu の Node.js 22 / 24 でテスト、型チェック、文書、公開ファイル、ビルド、実起動スモークを実行する。CI ファイルの存在だけで実行成功としない。

`npm run lint` には既存 UI の指摘が残るため、現在の CI 成功条件とは分けて扱う。ソース公開は全 UAT 完了・無欠陥・商用運用品質を示すものではない。

## 依存監査

2026-09-12 の npm audit は 7 件（high 4 / moderate 2 / low 1）を報告。React RSC / vinext / Vite は修正版へ更新して再検証した。残る Cloudflare 開発ツールおよび間接依存（Miniflare / sharp / undici / ws 等）の指摘は未解消。npm audit --omit=dev でも shadcn の間接 undici に high 1 件が残る。開発依存はローカル起動にも使用するため、単に「開発用だから安全」とは判断しない。loopback 限定のプレビューであり、公開ホスティングや信頼できないデータの処理へ拡張する前に、互換性確認を伴う追加更新が必要。
