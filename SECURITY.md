# セキュリティ方針

## 対象と境界

v0.1.0 は loopback 上の個人利用プレビュー。インターネットや LAN へ公開する構成をサポートしません。認証 off は OS アカウントに依存します。Google の許可リストは単一ワークスペースの入場制限で、テナント分離ではありません。

Google ID トークンは署名・クレーム・アカウントを Worker で検証します。Agent トークンは別途発行・失効する Bearer 資格情報です。Firebase からのログアウトで Agent トークンが自動失効するわけではありません。

## 保管

`.env`、`.dev.vars`、DB、実際の import、個人資料、バックアップを公開しないでください。Google Web 設定は秘密鍵ではありませんが、サンプルには実プロジェクトの値を入れません。外部 Agent へのデータ送信はそのクライアントとサービスの設定・規約にも依存します。

## 脆弱性の報告

GitHub の Security → Report a vulnerability が利用可能なら非公開で報告してください。利用できない場合は、公開 Issue に詳細・秘密・再現用個人データを載せず、非公開連絡方法を問い合わせてください。自分のローカル環境と架空データだけで検証してください。

受領期限や修正 SLA は設定していません。影響、再現条件、対象版、回避策を整理して優先度を判断します。依存関係の監査結果と、アプリの脅威境界における実際の影響を区別します。
